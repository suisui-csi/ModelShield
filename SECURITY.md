# 安全政策 · Security Policy

## 支持的版本

| 版本 | 是否维护 |
|---|---|
| v0.2.x | ✅ |
| < v0.2 | ❌ |

## 报告漏洞

**如果你在本项目自身发现安全漏洞**（例如：密钥泄露风险、命令注入、依赖漏洞），
请**不要**直接开公开 Issue，而是：

1. 通过 GitHub 的 **Security → Report a vulnerability** 私密提交，或
2. 邮件联系维护者（见仓库主页）

我们会在 **72 小时内**响应，并在修复后与你协商公开披露时间。

### 请提供

- 漏洞类型与影响范围
- 复现步骤（最小化）
- 受影响的版本/commit
- 你建议的修复方向（可选）

## ⚠️ 关于攻击语料的重要说明

本项目包含用于**安全评测**的攻击载荷（提示词注入、越狱等）。这些内容：

- **仅用于对你拥有或被授权的 AI 应用进行防御性评估**
- 载荷中被刻意设计为**不包含可直接用于犯罪的操作细节**（我们测的是"AI 会不会答应"）
- 若你发现某条语料包含了真实可利用的危险信息，**请立即通过上述渠道报告**，我们会尽快移除

## 使用者的责任

使用本工具即表示你确认：

1. 你只对**自己拥有或获得明确授权**的 AI 应用发起测试
2. 你遵守所在地区的法律法规
3. 你理解未经授权的安全测试可能构成违法

本项目作者不对任何滥用行为承担责任。

## 密钥安全（维护者自查清单）

- [ ] 仓库中不存在 `config.json` / `.token` / `.env` 等含密钥的文件
- [ ] `.gitignore` 覆盖 `.token*`、`*.token`、`*token*.txt`、`*secret*`
- [ ] 推送前运行 `python tools/verify_push.py` 做密钥扫描
- [ ] 一旦发现密钥泄露：立即吊销该密钥 → 从历史中清除 → 检查 GitHub 密钥扫描告警
